<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jsonMapping">

	



	<!-- FNAL INST 대분류 -->
	<select id="getFinalInst" resultType="String">
		SELECT DISTINCT g1 FROM final_inst
	</select>

	<!-- FNAL LOCA 대분류 -->
	<select id="getFinalLoca" resultType="String">
		SELECT DISTINCT g1 FROM final_loca
	</select>





</mapper>